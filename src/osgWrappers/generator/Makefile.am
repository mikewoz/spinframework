# Need to have a makefile in a subdir just to ensure that the wrappers
# are built before we try to compile them

all-local:
	@echo "******** Generating wrappers"
	cd $(top_srcdir) && ./buildWrappers.sh
